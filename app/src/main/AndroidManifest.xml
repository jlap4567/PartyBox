<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.partybox"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


    <uses-permission android:name="android.permission.INTERNET" >
    </uses-permission>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" >
    </uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivityNewPartyFirst"></activity>
        <activity android:name=".MadePartyListActivity"></activity>
        <activity android:name=".ViewEditPartyActivity"></activity>
        <activity android:name=".CommitAndPayActivity"></activity>
        <activity android:name=".CheckoutActivity"></activity>
        <activity android:name=".FoodListActivity"></activity>
        <activity android:name=".enterAddress"></activity>
        <activity android:name=".OrderCompleteActivity"></activity>

    </application>

</manifest>